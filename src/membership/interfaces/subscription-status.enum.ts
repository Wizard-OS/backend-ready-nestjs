export enum SubscriptionStatus {
  incomplete = 'incomplete',
  trialing = 'trialing',
  active = 'active',
  pastDue = 'past_due',
  suspended = 'suspended',
  canceled = 'canceled',
  expired = 'expired',
}
